#ifndef GRAPH INTERFACE #define GRAPH INTERFACE

template

class GraphInterface{ public:

virtual int getNum Vertices() const = 0;

virtual int getNumEdges () const =

0;

O

virtual void add(Label Type start, 2 LabelType end) = 0;

virtual bool remove (LabelType

4 start, LabelType end) = 0; 5 virtual int getEdgeWeight (LabelType 5 start, LabelType end) const = 0;

7

3 virtual void display()= 0;

#endif

B int edgeWeight;

1

5 int numOfEdges;

7int numOfVertices;

public:

O Graph (Label Type n);

2 int getNum Vertices() const;

int getNumEdges () const;

5

int getEdgeWeight (LabelType start, 5 Label Type end) const;

7

void add(LabelType start, LabelType 3 end);

bool remove (LabelType start,
